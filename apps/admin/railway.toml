# funnel-admin ” Next.js admin console (standalone output).
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/admin/Dockerfile"

[deploy]
startCommand = "node apps/admin/server.js"
healthcheckPath = "/api/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1

[deploy.resources]
cpu = 0.5
memoryMB = 512

# Custom domains:
#   admin.gofunnelai.com          â†’ production
#   admin.staging.gofunnelai.com  â†’ staging
